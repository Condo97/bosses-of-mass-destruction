jdk:
  - openjdk17

install:
  - ./gradlew assemble

script:
  - ./gradlew clean build publishToMavenLocal

publish:
  - mvn install:install-file -Dfile=build/libs/your-artifact.jar -DgroupId=com.github.your-username -DartifactId=your-artifact -Dversion=${version} -Dpackaging=jar